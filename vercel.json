{
  "buildCommand": "npm install && npm run web:build",
  "outputDirectory": "web-build",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
